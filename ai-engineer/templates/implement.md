Act as the implementor. Execute `ai/PLAN.md` exactly.

Red first: write the planned tests, watch them fail, then implement
until they pass. Commit code in logical units. Write `ai/REPORT.md`
covering: what changed per plan step, with commits; the verification
command and its decisive output; and every deviation from the plan or
modification to a test after its red phase, each with its
justification — "none" when clean, never omitted.
